apiVersion: v1
kind: Service
metadata:
  name: elearning-service
spec:
  type: NodePort
  selector:
    app: elearning
    version: blue
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30002

