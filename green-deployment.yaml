apiVersion: apps/v1
kind: Deployment
metadata:
  name: elearning-green
spec:
  replicas: 1
  selector:
    matchLabels:
      app: elearning
      version: green
  template:
    metadata:
      labels:
        app: elearning
        version: green
    spec:
      containers:
      - name: elearning
        image: sweetyraj22/elearning-site:latest
        ports:
        - containerPort: 80

